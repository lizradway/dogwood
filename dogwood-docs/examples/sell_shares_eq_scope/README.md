# sell_shares_eq_scope

An `==` scope constraint pinning the action to a specific entity reference: this
rule can only apply to `Drupe::Action::"SellShares"`. The `== EntityRef` form
on the `action` dimension requires an action reference (no template slots
allowed).

Referenced by `guide/02-policy-language.md` — The Policy Language.
