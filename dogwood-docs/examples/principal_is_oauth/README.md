# principal_is_oauth

Demonstrates the `principal is Type` entity-type scope constraint: the policy
only applies when the principal is an `Drupe::OAuthUser` entity (and the
action is `GetStockInfo`).

Referenced by `guide/02-policy-language.md` — The Policy Language.
