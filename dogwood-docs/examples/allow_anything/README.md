# allow_anything

An unconstrained (bare) scope triple: all three slots (`principal`, `action`,
`resource`) carry no constraint, so the rule matches **every** request.

Referenced by `guide/02-policy-language.md` — The Policy Language.
