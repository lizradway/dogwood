{
  "name": "dogwood-wasm-dev",
  "private": true,
  "version": "1.0.0",
  "description": "Build and test harness for the Dogwood TypeScript/WebAssembly bindings. The publishable package is the generated pkg/ (see pkg/package.json).",
  "scripts": {
    "build": "wasm-pack build --release --target nodejs --out-dir pkg",
    "build:bundler": "wasm-pack build --release --target bundler --out-dir pkg-bundler",
    "build:web": "wasm-pack build --release --target web --out-dir pkg-web",
    "test": "node test.cjs && node test-live.cjs && node test-coverage.cjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "verify": "npm run test && npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
