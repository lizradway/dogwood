# 0300 formerly within short window

First temporal case: a `permit` narrowed by an `unless temporal { ... }`
block using the past-only `formerly within` operator. Exercises the
temporal extension parser (slice 1). Verdict semantics arrive with the
evaluator (slice 3).
