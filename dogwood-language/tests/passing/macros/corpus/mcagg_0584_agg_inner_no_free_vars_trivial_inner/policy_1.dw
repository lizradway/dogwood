def temporal let_(?n, ?A, ?B) {
    exists (?n: Long). (?A == ?n && ?B)
};

permit (
    principal,
    action == Drupe::Action::"Alert",
    resource
)
when temporal {
    let_(n, count for (t: Timepoint). where (Drupe::Action::"Heartbeat"::request{ input.server: context.input.server } && tp(t)), n == 0)};
