def temporal let_(?n, ?A, ?B) {
    exists (?n: Long). (?A == ?n && ?B)
};

permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
unless temporal {
    let_(n, count for (t: Timepoint). where (formerly within 1h (Drupe::Action::"Read"::request{ input.user: context.input.user } && tp(t))), n > 2)};
