// Provider rule scoped to Read only; Audit is untouched by any provider.
permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when guardrails {
    Strings::Length(context.input.document).length < 6
};
