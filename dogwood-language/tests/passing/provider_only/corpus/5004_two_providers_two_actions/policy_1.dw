// Provider rule #1, scoped to Read: short documents only.
permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when guardrails {
    Strings::Length(context.input.document).length < 6
};
