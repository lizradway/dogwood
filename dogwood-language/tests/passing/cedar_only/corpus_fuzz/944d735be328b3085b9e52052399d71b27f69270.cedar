forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource in a::""
) when {
  ((true && ("\0" like "\0*")) && (if ("\0" like "\0*") then ("\0" like "\0*") else ("\0" like "\0*"))) && ("\0" like "\0*")
};
