forbid(
  principal is jz::a,
  action == jz::Action::"action",
  resource in jz::a::">>>"
) when {
  (true && (if ((decimal("")).lessThan(decimal(""))) then ((decimal("")).lessThan(decimal(""))) else false)) && false
};
