@Mg("")
@Mj("")
forbid(
  principal is a in a::"",
  action == Action::"action",
  resource == a::""
) when {
  ((true && (action in [action, action, action])) && (action in [action, action, action])) && (action in [action, action, action])
};
