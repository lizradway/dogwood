permit(
  principal,
  action,
  resource
) when {
  (true && (if (resource is a) then (if (!(((datetime("0061-80-01")).offset(duration("0ms"))) < (datetime("0000-01-01")))) then false else false) else false)) && false
};
