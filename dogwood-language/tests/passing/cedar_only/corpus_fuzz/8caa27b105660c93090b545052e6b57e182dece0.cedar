forbid(
  principal == a::"",
  action == Action::"}}c",
  resource
) when {
  (true && (if (if (context has "") then (context has "") else (context has "")) then (if (if true then false else true) then false else false) else false)) && false
};
