namespace A::A {
  entity a;

  action "" in [A::A::Action::"VV", A::A::Action::"                                          ", A::A::Action::"  ", A::A::Action::"   "] appliesTo {
    principal: [a],
    resource: [a],
    context: {}
  };

  action "  " in [A::A::Action::"   "];

  action "   ";

  @S00000
  action "                                          " in [A::A::Action::"  ", A::A::Action::"   "] appliesTo {
    principal: [a],
    resource: [a],
    context: {}
  };

  action "VV" in [A::A::Action::"                                          ", A::A::Action::"  ", A::A::Action::"   "] appliesTo {
    principal: [a],
    resource: [a],
    context: {
      Ln00140?: {      }
    }
  };
}

