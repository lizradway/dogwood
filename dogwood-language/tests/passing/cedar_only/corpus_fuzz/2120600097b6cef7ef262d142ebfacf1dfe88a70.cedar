permit(
  principal,
  action,
  resource
) when {
  (true && ((if ([].containsAny([false, false])) then (if false then [] else []) else []).containsAny([[].containsAny([]), [].containsAny([])]))) && false
};
