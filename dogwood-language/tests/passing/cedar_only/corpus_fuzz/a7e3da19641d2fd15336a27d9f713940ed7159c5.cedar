permit(
  principal,
  action,
  resource
) when {
  (true && (if (resource in resource) then (if (if true then true else true) then (context[""]) else (context.de)) else ((if false then context else context)[""]))) && false
};
