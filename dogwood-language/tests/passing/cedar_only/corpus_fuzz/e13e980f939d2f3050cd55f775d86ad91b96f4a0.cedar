forbid(
  principal is a in a::"\0\u{e}\u{e}\0",
  action == Action::"p",
  resource
) when {
  ((true && ((ip(if false then "" else "")).isMulticast())) && ((true && (action in action)) && (principal in principal))) && (principal in principal)
};
