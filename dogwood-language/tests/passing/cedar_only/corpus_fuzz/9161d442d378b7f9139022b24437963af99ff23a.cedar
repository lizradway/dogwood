permit(
  principal,
  action,
  resource
) when {
  ((true && ((if (false == false) then (decimal("\n\u{13}\0\0\0", "D\0", "")) else (decimal("", "", "\0\0\0\0\0\0\0\0", ""))).lessThan(decimal("3355.4432")))) && false) && false
};
