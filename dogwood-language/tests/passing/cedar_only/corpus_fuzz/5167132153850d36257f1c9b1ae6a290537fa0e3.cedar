permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (if (principal in principal) then (principal in principal) else (principal in principal))) && (principal in principal)) && (principal in principal)
};
