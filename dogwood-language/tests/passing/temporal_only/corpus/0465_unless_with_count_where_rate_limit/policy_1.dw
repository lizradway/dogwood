permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
unless temporal {
    exists (n: Long). ((count for (t: Timepoint). where (formerly within 1h (Drupe::Action::"Read"::request{ input.user: context.input.user } && tp(t)))) == n && n > 2)};
