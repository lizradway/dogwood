permit (
    principal,
    action == Drupe::Action::"Transfer",
    resource
)
when temporal {
    formerly within 1h (Drupe::Action::"Transfer"::request{ input.user: context.input.user, input.amount: _ } && context.input.user == "")
};
