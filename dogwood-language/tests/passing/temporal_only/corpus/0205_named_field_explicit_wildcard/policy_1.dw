// Explicit wildcard in named-field syntax: `role: _` means any role.

permit (
    principal,
    action == Drupe::Action::"Access",
    resource
)
when temporal {
    formerly within 1h Drupe::Action::"Grant"::response{ input.role: _, input.user: context.input.user, output.ok: true }
};
