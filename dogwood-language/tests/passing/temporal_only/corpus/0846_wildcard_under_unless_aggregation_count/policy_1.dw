permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
unless temporal {
    exists (n: Long). ((count for (t: Timepoint). where (Drupe::Action::"Login"::response{ input.user: context.input.user, input.server: _, output.result: true } && tp(t))) == n && n >= 2)};
