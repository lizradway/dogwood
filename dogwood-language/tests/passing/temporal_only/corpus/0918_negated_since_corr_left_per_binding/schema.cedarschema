namespace Drupe {
  type TransferInput = {
    amount: Long,
    user: String
  };

  type TransferOutput = {  };

  entity Gateway;

  entity OAuthUser = {
    id: String
  };

  action "Transfer" appliesTo {
    principal: [OAuthUser],
    resource: [Gateway],
    context: {
      input: TransferInput,
      output?: TransferOutput
    }
  };

  action "Approve" appliesTo {
    principal: [OAuthUser],
    resource: [Gateway],
    context: {
      input: TransferInput,
      output?: TransferOutput
    }
  };
}
