// Ordinary login-gated read; the schema shape (context declared via a
// common-type reference) is what this case exercises.
permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when temporal {
    formerly within 1h Drupe::Action::"Login"::request{ input.user: context.input.user }
};
