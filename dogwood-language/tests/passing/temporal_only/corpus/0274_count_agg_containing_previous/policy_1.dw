permit (
    principal,
    action == Drupe::Action::"Transfer",
    resource
)
when temporal {
    exists (n: Long). ((count for (t: Timepoint). where (previous within 1h (Drupe::Action::"Login"::request{ input.user: context.input.user } && tp(t)))) == n && n > 0)};
