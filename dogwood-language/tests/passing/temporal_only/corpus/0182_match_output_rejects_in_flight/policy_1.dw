// Requires a response with result == true. An in-flight request
// has no response at all — should NOT fire.

permit (
    principal,
    action == Drupe::Action::"Logout",
    resource
)
when temporal {
    formerly within 1h Drupe::Action::"Login"::response{ input.user: context.input.user, output.result: true }
};
