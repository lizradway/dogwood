permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when temporal {
    formerly within 1h (Drupe::Action::"Transfer"::request{ input.user: context.input.user, input.amount: context.input.amount, input.limit: context.input.limit } && context.input.amount >= context.input.limit)
};
