permit (
    principal,
    action == Drupe::Action::"Alert",
    resource
)
when temporal {
    exists (n: Long). ((count for (t: Timepoint). where (Drupe::Action::"Login"::request{ input.user: context.input.server, input.server: context.input.server } && tp(t))) == n && n == 0)};
