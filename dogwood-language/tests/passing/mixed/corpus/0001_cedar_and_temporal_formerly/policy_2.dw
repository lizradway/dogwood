// Forbid Write unless the user has logged in within the last hour
forbid (
    principal,
    action == Drupe::Action::"Write",
    resource
)
unless temporal {
    formerly within 1h Drupe::Action::"Login"::request{ input.user: context.input.user }
};
