permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when temporal {
    let n = count where (
        without Drupe::Action::"Heartbeat"::request{ input.user: context.input.user }
        since within 1h
        Drupe::Action::"Login"::request{ input.user: context.input.user, input.server: context.input.server }
    ) in n >= 2
};
