permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when temporal {
    let total = sum r where (previous within 1h (Drupe::Action::"Compute"::response{ input.user: context.input.user, output.result: r })) in total >= 200
};
