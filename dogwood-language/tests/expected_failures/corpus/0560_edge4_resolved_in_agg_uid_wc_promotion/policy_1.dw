forbid (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when temporal {
    let n = count where (formerly within 1h Drupe::Action::"Login"::response{ input.user: context.input.user, input.server: _, output.result: _ }) in n > 0
};
