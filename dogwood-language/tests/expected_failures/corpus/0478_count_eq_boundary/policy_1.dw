permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when temporal {
    let n = count where (formerly within 1h Drupe::Action::"Login"::request{ input.user: context.input.user, input.server: _ }) in n == 2
};
