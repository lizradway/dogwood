aggregation `for`-variable `w` is not range-restricted by any positive atom in the `where` body; bind it with a predicate field (`P{ f: w }`), a `tp(w)`, or an equality (`(count …) == w`) — an occurrence under a negation or in the left operand of a `since` does not restrict
